spring:
  application:
    name: JPANov
  datasource:
    url: jdbc:h2:./appdb
    username: sa
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true
server:
  port: 8082
