spring:
  application:
    name: OrderAPP

  datasource:
    url: jdbc:h2:./appdb
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 7030

